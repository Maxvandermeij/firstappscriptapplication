function orderofthefunctions() {
  
  var year = 2019;
  
  cleanSheets();
 
  setAllBasicSheets();
  setDates(year);
  setFormatting(year);
  setWeeklyHourFormula(year);
  
  setConditionalFormatting(year);
  //datavalidation rule (alleen cijfers)
  //validationnumbersofhours
  //conditionalformatting (0's, job confirmation)
  //protectcells
}